f = 17 * exp(x^2);
dom = [-1b5; 1b5];
target = 1b-64; 
maxDegree = 16;
minWidth = (sup(dom) - inf(dom)) * 1/256;
tableIndexWidth = 8;
minimalReductionRatio = 1000;
metaSplitMinWidth = (sup(dom) - inf(dom)) * 1/64;

