f = 2^x + sin(pi * x) + 2;
dom = [-40; 40];
target = 1b-30;
maxDegree = 8;
minWidth = (sup(dom) - inf(dom)) * 1/32;
tableIndexWidth = 9;
minimalReductionRatio = 1000;
metaSplitMinWidth = (sup(dom) - inf(dom)) * 1/1024;

